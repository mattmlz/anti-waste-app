# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'anti-waste' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for anti-waste
  pod 'SwiftyOnboard'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire'

  target 'anti-wasteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'anti-wasteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
